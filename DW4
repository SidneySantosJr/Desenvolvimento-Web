var cost = document.querySelector('body > table > tbody > tr:nth-child(3) > td > form > div.divContentBox > div.divContentBoxBody > table > tbody');

var lista = cost.querySelectorAll('tr');

var dados = Array.from(lista).map(linha => { const celula = linha.querySelectorAll('td');
return Array.from(celula).map(celula => celula.innerText.trim());
                                           });



const resultado = JSON.stringify(dados, null, 2);
copy(resultado);
